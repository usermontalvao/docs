# Copie este arquivo para .env.server e preencha os valores reais.
#   cp .env.server.example .env.server
#
# NÃO comite o .env.server (ele está no .gitignore) — contém a chave de licença.

# Subdomínio DEDICADO deste serviço. O DNS deste nome deve apontar (A/AAAA)
# para o IP público do host onde o Docker roda. O Caddy emite o certificado
# TLS automaticamente para ele.
DOCX_DOMAIN=docx.seudominio.com.br

# E-mail para o Let's Encrypt (avisos de expiração de certificado).
ACME_EMAIL=voce@seudominio.com.br

# Chave de licença do Syncfusion.
# ATENÇÃO: precisa ser uma licença que cubra o Word Processor SERVER-SIDE
# (Document Processing / DocIO), não só o editor JS do frontend.
# Gere/confirme no portal: https://www.syncfusion.com/account/downloads
SYNCFUSION_LICENSE_KEY=COLE_SUA_LICENSE_KEY_AQUI

# (OPCIONAL) Chave compartilhada exigida no header X-Api-Key.
# Deixe VAZIO para manter o gate desligado (o editor funciona sem ela).
# Se preencher, descomente o bloco @unauthorized no Caddyfile E faça o frontend
# enviar o header (veja DEPLOY.md > Segurança). Lembre: num SPA a chave vai no
# bundle do navegador, então serve só contra abuso casual, não atacante dedicado.
DOCX_API_KEY=
